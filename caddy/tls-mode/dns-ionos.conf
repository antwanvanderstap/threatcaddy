# ACME DNS-01 against IONOS. Validation happens through a TXT record, so this
# works with no inbound connectivity at all — the case a residential ISP that
# blocks port 80 forces you into — while still yielding a publicly trusted
# certificate that needs nothing installed on any client.
dns ionos {$TC_IONOS_API_TOKEN}
